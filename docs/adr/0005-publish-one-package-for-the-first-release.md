# Publish one package for the first release

The first release will publish a single `Bzs.Blazor` Razor Class Library containing internally separated component, theme, icon, interop, and service modules. Tests and the demo application remain separate projects, while adapter packages and finer NuGet splits are deferred until independent dependencies, consumers, or release cadences make those seams real.
