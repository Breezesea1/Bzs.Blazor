# Domain Docs

This is a single-context repository.

Before exploring or changing the codebase, read:

- `CONTEXT.md`
- the ADRs under `docs/adr/` that affect the current area

Use the glossary vocabulary exactly. If a proposal conflicts with an ADR, surface the conflict explicitly instead of silently overriding it.
